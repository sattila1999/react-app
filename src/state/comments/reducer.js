import { exampleComments } from "../../domain/comments";

const initialState = exampleComments;

export const commentsReducer = (state = initialState, action) => {
  //const { type, payload } = action;

  return state;
};
