import { examplePosts } from "../../domain/posts";

const initialState = examplePosts;

export const postsReducer = (state = initialState, action) => {
  //const { type, payload } = action;

  return state;
};
